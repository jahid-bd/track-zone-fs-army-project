import styled from "styled-components";
import colors from "../colors/Colors";

const ClientClockContainer = styled.div`
  background-color: ${colors.secondery};
  padding: 15px 25px;
  border: 3px solid #112d4e;
  border-radius: 5px;
  text-align: center;
`;

export default ClientClockContainer;
